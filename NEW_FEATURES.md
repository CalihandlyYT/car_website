# 🚀 Новые функции

## ✅ Реализованные функции

### 1. **Сравнение 2 автомобилей**
- Маршрут: `/compare?post1=ID&post2=ID`
- Позволяет сравнить два поста рядом
- Шаблон: `templates/compare.html`

### 2. **Множественные фото для постов**
- При создании/редактировании поста можно добавить дополнительные изображения
- Поле формы: `additional_images[]`
- Хранятся в таблице `post_images`

### 3. **Теги для постов**
- Можно добавлять теги при создании/редактировании поста
- Поле формы: `tags[]`
- Хранятся в таблицах `tags` и `post_tags`

### 4. **Фильтрация по тегам**
- URL: `/?tag=название_тега`
- Фильтрация работает на главной странице

### 5. **Категории автомобилей**
- Доступные категории: седан, купе, универсал, хэтчбек, кроссовер, внедорожник, кабриолет, пикап, микроавтобус
- Поле формы: `category`
- Фильтрация: `/?category=название_категории`

### 6. **Избранное**
- Маршруты:
  - `/favorites` - список избранных постов
  - `/toggle_favorite/<post_id>` (POST) - добавить/удалить из избранного
- Шаблон: `templates/favorites.html`

### 7. **Подписка на авторов**
- Маршруты:
  - `/toggle_subscription/<author_email>` (POST) - подписаться/отписаться
  - `/subscriptions` - посты от подписок
- Шаблон: `templates/subscriptions.html`
- Автоматические уведомления при новых постах

### 8. **Уведомления**
- Маршруты:
  - `/notifications` - список уведомлений
  - `/notifications/mark_seen/<id>` (POST) - отметить как прочитанное
  - `/notifications/mark_all_seen` (POST) - отметить все как прочитанные
  - `/notifications/count` - количество непрочитанных
- Шаблон: `templates/notifications.html`
- Типы уведомлений: `new_post` (новый пост от подписки)

### 9. **Жалобы (Пожаловаться)**
- Маршрут: `/report` (POST)
- Параметры:
  - `target_type`: 'post' или 'comment'
  - `target_id`: ID поста или комментария
  - `reason`: причина жалобы
- Жалобы хранятся в таблице `reports` со статусом 'pending'

## 📊 Новые таблицы в БД

1. **post_images** - дополнительные изображения постов
2. **tags** - список тегов
3. **post_tags** - связь постов и тегов
4. **favorites** - избранные посты пользователей
5. **subscriptions** - подписки на авторов
6. **notifications** - уведомления пользователей
7. **reports** - жалобы на посты/комментарии

## 🔧 Обновленные функции

- `create_post()` - теперь поддерживает category, images, tags
- `update_post()` - обновлена для новых полей
- `get_all_posts()` - добавлена фильтрация по тегам и категориям
- `get_post_by_id()` - возвращает images и tags

## 📝 Что нужно обновить в шаблонах

### index.html
- Добавить фильтры по тегам и категориям
- Добавить кнопки "В избранное", "Подписаться", "Пожаловаться"
- Показать теги и категорию для каждого поста
- Показать счетчик непрочитанных уведомлений

### create_post.html и edit_post.html
- Добавить поле выбора категории
- Добавить поля для дополнительных изображений
- Добавить поле для тегов (можно с автодополнением)

### post.html
- Показать галерею изображений (основное + дополнительные)
- Показать теги и категорию
- Добавить кнопки: "В избранное", "Подписаться", "Пожаловаться"
- Добавить кнопку "Сравнить" (для выбора второго поста)

## 🎯 Примеры использования

### Добавить в избранное (JavaScript)
```javascript
fetch('/toggle_favorite/123', {method: 'POST'})
  .then(r => r.json())
  .then(data => console.log(data));
```

### Подписаться на автора (JavaScript)
```javascript
fetch('/toggle_subscription/user@example.com', {method: 'POST'})
  .then(r => r.json())
  .then(data => console.log(data));
```

### Пожаловаться на пост (JavaScript)
```javascript
const formData = new FormData();
formData.append('target_type', 'post');
formData.append('target_id', '123');
formData.append('reason', 'Спам');

fetch('/report', {method: 'POST', body: formData})
  .then(r => r.json())
  .then(data => console.log(data));
```

### Получить количество уведомлений
```javascript
fetch('/notifications/count')
  .then(r => r.json())
  .then(data => console.log('Непрочитанных:', data.count));
```

## 🔐 Права доступа

- Все функции требуют авторизации
- Забаненные пользователи не могут использовать функции
- Жалобы доступны всем авторизованным пользователям
- Модераторы могут просматривать жалобы через админ-панель (нужно добавить)

## 📌 Примечания

- Email автора формируется как `author_name@example.com` (где author_name - имя из email пользователя)
- При создании нового поста автоматически отправляются уведомления подписчикам
- Все функции логируются в access.log

